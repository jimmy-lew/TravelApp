# App Firebase Setup & Relations

- Real-time Database
  - Data is stored in Firebase Real-time Database, no-sql DB that listens for changes and has instant updates

```mermaid
erDiagram
    User {
        string name
        string email
        string profilePicture
        string password
    }
```